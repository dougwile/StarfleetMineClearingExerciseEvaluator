using MineClearingEvaluator.Common;

namespace MineClearingEvaluator.Models
{
    public class Ship : Entity
    {
        public Ship(int x, int y, int z) : base(x, y, z)
        {
            
        }
    }
}